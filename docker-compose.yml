services:
  app:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./src:/app
    environment:
      - DASH_DEBUG=true
    command: ["uv", "run", "gunicorn", "--workers", "2", "--bind", "0.0.0.0:8050", "--reload", "app:server"]
